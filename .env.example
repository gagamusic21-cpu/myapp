# ---- Hawassa University Exams & Courses — frontend environment ----

# Base URL of the deployed API (Render). Leave empty to run the site
# in local device mode (no backend, content stored in the browser).
VITE_API_URL=http://localhost:4000
